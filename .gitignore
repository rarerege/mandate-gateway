__pycache__/
*.pyc
.venv/
venv/
.env
.pytest_cache/
*.egg-info/

# Mutable runtime state — always regenerated by app.db.init_db() /
# scripts/seed_demo.py. NOT ignored: data/reputation_model.joblib and
# data/demo_keys.json are committed on purpose so `git clone` + install +
# run works immediately without a training step.
data/*.db
data/*.sqlite3
